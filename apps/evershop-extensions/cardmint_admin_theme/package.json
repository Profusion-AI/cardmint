{
  "name": "@cardmint/admin-theme",
  "version": "1.0.0",
  "type": "module",
  "description": "CardMint brand theming for EverShop admin dashboard",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r pages src/* dist/",
    "clean": "rm -rf dist"
  }
}
